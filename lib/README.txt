Scaricare il driver JDBC per SQLite (sqlite-jdbc-<versione>.jar) da:
https://mvnrepository.com/artifact/org.xerial/sqlite-jdbc
e posizionarlo in questa cartella (lib/) prima di compilare.

In questo caso già è stato caricato e posizionato correttamente.

